# HTTP-контракт frontend/backend — MVP v2

Согласованные изменения: **все клиентские идентификаторы в HTTP JSON — десятичные строки; загрузка только Parquet**. Корневая копия docs/data_contract.md пока не обновляется и содержит старый контракт. Для текущей интеграции используйте этот документ.

API: `http://127.0.0.1:8000`; Swagger: `http://127.0.0.1:8000/docs`.
Frontend работает отдельно; CORS задаётся списком origins через CORS_ORIGINS.

Не преобразовывайте gid/src/dst в JavaScript Number. Сравнивайте и передавайте их как строки. Это касается узлов, рёбер, карточки, top_nodes и clusters.top_gids. cluster_id, depth, rank, n_tx и счётчики — JSON-числа. Суммы в KZT — JSON-числа для отображения; HTTP не гарантирует дополнительную точность денег относительно float64 источника. Domain хранит int/Decimal, CSV содержит точные десятичные gid.

## Сценарий

1. GET /api/health или GET /api/analysis. До загрузки анализа нет.
2. POST /api/analyze с тремя Parquet-файлами; дождаться успешного расчёта.
3. GET /api/analysis возвращает полный граф, включая изолированные узлы, кластеры и топ.
4. GET /api/nodes/{gid} возвращает карточку и все её направленные связи.
5. GET /api/exports/{name} скачивает CSV текущего снимка.

POST /api/ask не реализован; AI не подключён.

## GET /api/health

200, до первой загрузки:
```json
{
  "status": "ok",
  "analysis_ready": false,
  "ai_configured": false
}
```

После успешной загрузки analysis_ready=true. ai_configured всегда false.

## POST /api/analyze

multipart/form-data, ровно три файловых поля, без дубликатов и дополнительных полей:

| Поле | Обязательные Parquet-колонки |
|---|---|
| nodes | gid, depth, is_seed |
| edges | src, dst, sum_kzt, n_tx, depth |
| transactions | src, dst, date, sum_kzt |

Имена приложенных файлов должны оканчиваться на .parquet; содержимое проверяется как Parquet. Клиентские имена не используются как пути. Лимит 25 MiB (26 214 400 байт) на файл проверяется по фактически прочитанным байтам. Общий HTTP body ограничен 76 MiB с учётом multipart. Типы, пропуски и правила выборки проверяются существующим validate; дополнительные колонки не используются.

Успех — 200 после полного расчёта и подготовки CSV. Пример ниже рассчитан на синтетическом наборе из четырёх узлов, включая два различных gid, которые нельзя точно хранить как Number:
```json
{
  "analysis_id": "example-analysis",
  "status": "ready",
  "summary": {
    "n_nodes": 4,
    "n_edges": 3,
    "n_transactions": 4,
    "n_seed": 2,
    "n_clusters": 2,
    "edge_volume_kzt": 20000.0
  },
  "analysis_url": "/api/analysis"
}
```

Числа не фиксированы. Для выданного набора ожидаются 2248 узлов, 3119 рёбер, 4840 транзакций, 81 seed; число кластеров и оборот возвращаются по расчёту.

analysis_id меняется при каждом успешном расчёте. При ошибке прежний снимок и его CSV остаются доступны. Параллельный пересчёт возвращает 409 ANALYSIS_BUSY; GET-запросы продолжают отдавать предыдущий снимок.

## GET /api/analysis

200: analysis_id, summary, nodes, edges, clusters, top_nodes. Все узлы и исходные направленные рёбра присутствуют, пагинации нет. Никакие gid не проходят через float. Полный пример для того же синтетического набора:
```json
{
  "analysis_id": "example-analysis",
  "summary": {
    "n_nodes": 4,
    "n_edges": 3,
    "n_transactions": 4,
    "n_seed": 2,
    "n_clusters": 2,
    "edge_volume_kzt": 20000.0
  },
  "nodes": [
    {
      "gid": "100000000000000001",
      "depth": 0,
      "is_seed": true,
      "role": "peripheral",
      "role_score": 0.5,
      "cluster_id": 1,
      "priority_score": 0.85,
      "evidence": "Признаки явной роли не выявлены: входов=1, выходов=2; seed: входящие неполные, out/in не трактуется",
      "in_deg": 1,
      "out_deg": 2,
      "in_kzt": 5000.0,
      "out_kzt": 15000.0,
      "truncated_by_depth": false
    },
    {
      "gid": "100000000000000002",
      "depth": 1,
      "is_seed": false,
      "role": "peripheral",
      "role_score": 0.5,
      "cluster_id": 1,
      "priority_score": 0.8854450335201939,
      "evidence": "Признаки явной роли не выявлены: входов=1, выходов=1; полный баланс неизвестен",
      "in_deg": 1,
      "out_deg": 1,
      "in_kzt": 10000.0,
      "out_kzt": 5000.0,
      "truncated_by_depth": false
    },
    {
      "gid": "100000000000000003",
      "depth": 4,
      "is_seed": false,
      "role": "peripheral",
      "role_score": 0.5,
      "cluster_id": 1,
      "priority_score": 0.6476779968915591,
      "evidence": "Признаки явной роли не выявлены: входов=1, выходов=0; depth=4: граница наблюдения, не доказанный сток",
      "in_deg": 1,
      "out_deg": 0,
      "in_kzt": 5000.0,
      "out_kzt": 0.0,
      "truncated_by_depth": true
    },
    {
      "gid": "100000000000000004",
      "depth": 0,
      "is_seed": true,
      "role": "peripheral",
      "role_score": 0.5,
      "cluster_id": 2,
      "priority_score": 0.0,
      "evidence": "Признаки явной роли не выявлены: входов=0, выходов=0; seed: входящие неполные, out/in не трактуется",
      "in_deg": 0,
      "out_deg": 0,
      "in_kzt": 0.0,
      "out_kzt": 0.0,
      "truncated_by_depth": false
    }
  ],
  "edges": [
    {
      "src": "100000000000000001",
      "dst": "100000000000000002",
      "sum_kzt": 10000.0,
      "n_tx": 2
    },
    {
      "src": "100000000000000002",
      "dst": "100000000000000001",
      "sum_kzt": 5000.0,
      "n_tx": 1
    },
    {
      "src": "100000000000000001",
      "dst": "100000000000000003",
      "sum_kzt": 5000.0,
      "n_tx": 1
    }
  ],
  "clusters": [
    {
      "cluster_id": 1,
      "n_nodes": 3,
      "n_seed": 1,
      "sum_kzt_internal": 20000.0,
      "top_gids": [
        "100000000000000002",
        "100000000000000001",
        "100000000000000003"
      ],
      "hypothesis": "Группа наблюдаемых потоков: узлов=3, seed=1; признаки сбора у 0, распределения у 0, координации у 0; внутренний оборот=20000.0 KZT. Гипотеза для проверки, не доказательство общей деятельности"
    },
    {
      "cluster_id": 2,
      "n_nodes": 1,
      "n_seed": 1,
      "sum_kzt_internal": 0.0,
      "top_gids": [
        "100000000000000004"
      ],
      "hypothesis": "Изолированный узел: наблюдаемых связей=0; назначение группы не установлено"
    }
  ],
  "top_nodes": [
    {
      "rank": 1,
      "gid": "100000000000000002",
      "role": "peripheral",
      "priority_score": 0.8854450335201939,
      "why": "Связей=2 (27.7% балла); операций=3 (21.5%); оборот=15000.0 KZT (24.3%); соседей seed=1 (15.0%). Приоритет проверки, не вывод о виновности."
    },
    {
      "rank": 2,
      "gid": "100000000000000001",
      "role": "peripheral",
      "priority_score": 0.85,
      "why": "Связей=3 (35.0% балла); операций=4 (25.0%); оборот=20000.0 KZT (25.0%); соседей seed=0 (0.0%). Приоритет проверки, не вывод о виновности."
    },
    {
      "rank": 3,
      "gid": "100000000000000003",
      "role": "peripheral",
      "priority_score": 0.6476779968915591,
      "why": "Связей=1 (17.5% балла); операций=1 (10.8%); оборот=5000.0 KZT (21.5%); соседей seed=1 (15.0%). Приоритет проверки, не вывод о виновности."
    },
    {
      "rank": 4,
      "gid": "100000000000000004",
      "role": "peripheral",
      "priority_score": 0.0,
      "why": "Связей=0 (0.0% балла); операций=0 (0.0%); оборот=0 KZT (0.0%); соседей seed=0 (0.0%). Приоритет проверки, не вывод о виновности."
    }
  ]
}
```

До загрузки — 404 NO_ANALYSIS.

## GET /api/nodes/{gid}

Параметр gid — десятичная строка int64. Ответ содержит analysis_id, node, incoming, outgoing, limitations. Входящие связи имеют dst=gid, исходящие — src=gid; ограничения количества связей нет. Self-loop будет в обоих списках. Пример узла на границе наблюдения:
```json
{
  "analysis_id": "example-analysis",
  "node": {
    "gid": "100000000000000003",
    "depth": 4,
    "is_seed": false,
    "role": "peripheral",
    "role_score": 0.5,
    "cluster_id": 1,
    "priority_score": 0.6476779968915591,
    "evidence": "Признаки явной роли не выявлены: входов=1, выходов=0; depth=4: граница наблюдения, не доказанный сток",
    "in_deg": 1,
    "out_deg": 0,
    "in_kzt": 5000.0,
    "out_kzt": 0.0,
    "truncated_by_depth": true
  },
  "incoming": [
    {
      "src": "100000000000000001",
      "dst": "100000000000000003",
      "sum_kzt": 5000.0,
      "n_tx": 1
    }
  ],
  "outgoing": [],
  "limitations": [
    "Наблюдаются только внутрибанковские переводы за июль 2026 от 5000 KZT; полный баланс неизвестен",
    "Роль и оценки — гипотезы для проверки, не вероятность и не вывод о виновности",
    "depth=4 и отсутствие исходящих — граница наблюдения, не доказательство удержания"
  ]
}
```

Неизвестный gid — 404 GID_NOT_FOUND; неверная запись gid — 422 INVALID_SCHEMA; до загрузки — 404 NO_ANALYSIS.

## GET /api/exports/{name}

Разрешены только nodes_roles.csv, clusters.csv, top_nodes.csv. Успех — 200, Content-Type: text/csv; charset=utf-8, Content-Disposition: attachment; filename="...". Заголовок X-Analysis-Id указывает версию скачанного снимка и доступен frontend через CORS. Схемы CSV сохранены по ТЗ; top_gids в CSV — JSON-массив целых, а в HTTP — массив строк.

Неизвестное имя или путь — 404 EXPORT_NOT_FOUND. До загрузки разрешённого файла — 404 NO_ANALYSIS. Отдельные HTTP-запросы могут попасть на разные версии, если между ними закончился новый расчёт: сравнивайте analysis_id/X-Analysis-Id и обновляйте экран.

## Ошибки

Единый формат, без traceback и внутренних путей:

```json
{
  "error": {
    "code": "INVALID_SCHEMA",
    "message": "nodes.parquet: поле gid: обязательная колонка отсутствует или повторяется",
    "details": {"file": "nodes.parquet"}
  }
}
```

| HTTP | code | Причина |
|---|---|---|
| 404 | NO_ANALYSIS | Нет активного анализа |
| 404 | GID_NOT_FOUND | Неизвестный gid |
| 404 | EXPORT_NOT_FOUND | Недопустимый или отсутствующий экспорт |
| 422 | INVALID_SCHEMA | Форма, формат, схема или значения неверны |
| 413 | FILE_TOO_LARGE | Превышен лимит файла/запроса |
| 409 | ANALYSIS_BUSY | Другой анализ выполняется |
| 500 | INTERNAL_ERROR | Внутренняя ошибка без раскрытия деталей |

## Состояние MVP

Один процесс Uvicorn и один worker. Снимок хранится в памяти; после перезапуска нужна повторная загрузка. CSV находятся раздельно в out/api/<analysis_id>/. Старые версии сохраняются для уже начатых скачиваний; автоматической очистки версий пока нет. Временные загрузки удаляются после успеха и ошибок. Не изменяйте каталоги версий во время работы сервера.
