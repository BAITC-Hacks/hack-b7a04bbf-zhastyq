# Real API by default. Set true only for the explicitly labelled demo fixtures.
VITE_API_MOCK=false
# Empty: same-origin /api (Vite forwards it to http://127.0.0.1:8000 in development).
# Set the server origin here when the API is hosted separately; configure its CORS too.
VITE_API_BASE_URL=
